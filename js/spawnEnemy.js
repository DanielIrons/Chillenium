function spawnEnemy(enemyObj, grassPos) {
	var numberOf = Math.floor(Math.random() * log(grassPos));
	xValue = grassPos + Math.floor(Math.random() * 128);
	if(grassPos % 128)
	{	
		// load numberOf sprites
		// var enemy = new enemyType(index, gl, vs, fs, xValue);
	}
}